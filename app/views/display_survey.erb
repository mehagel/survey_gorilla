<h2><%= @survey.name%></h2>

<ul>
  <form action="/vote" method="post">
    <input type="hidden" name="survey" value= "<%=@survey.id%>"></input>
    <% @survey.questions.each do |question| %>
          <li><%= question.content %></li>
      <% question.choices.each do |choice| %>
    <p>
      <input type="radio" name="<%=question.id%>" value="<%= choice.id %>"><%=choice.content%></input>
    </p>
    <% end %>
    <% end %>
    <input type="submit" value="Submit"></input>
  </form>
</ul>

